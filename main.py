import random

def myFunction(numList, num):
          counter = 0
           for elem in range(numList):
               if elem < num
                   counter += 1
       # generate a list of 10 random integer.
       for i in range(10):
           list1.append(randint(0, 10))
       myFunction(list1)
       print(counter)